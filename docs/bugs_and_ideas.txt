#TODO: Cython implementation


# TODO ALL
    * delete users who blocked the bot and check for BadRequest when sending messages
    * update channels, groups and users username's
    * changed tokens and 2 bot instances running handlers
    * fix double admins bug
    * backs everywhere
    * move information of the list to the pagination text
    * maybe add dynamic changes using edit
    * when delete in inbox - don't delete all telegram messages and don't show it again.
    * Use one code for the updating database in blocks(inbox, user) and so on
    * Application factory
    * More information to the user list (active users count...)
    * handling admins that leave the bot
    Под каждым юзером в списке юзеров кнопка «открыть»
        (если юзер отписался показать что он отписался и кнопка «удалить»)
        - дальше меню с юзер инфой(донаты, покупки, сообщения, статусы)
        (удалить все сообщения, написать,
        история донатов, заблокировать, отключить сообщения)
    * create bot on competitor platform(and take features from it)
    * donations notifications(donate comment)
    * off/on message notification
    * Messages history
    * Protect all /start, backs, get_help, help_button and so on with user_data.clear() function call
    * When user click "Open" and "Hide" in message notification - it works bad

# TODO BUGS
    * None on greating (mb when greating comes from bot_father)
    * USER VIEW - ("anonim_messages_blocked" and "regular_messages_blocked")
      when superuser come to user view and write message and then block messaging for himself and then come to user view again
      ("anonim_messages_blocked" and "regular_messages_blocked") - this fields become False(instead of True)
    * stickers bug in bot_father reports(check stickers for crowd)
    * имя и username ансабов не обновляется в списке юзеров
    * if subscribers leave telegram - check for exceptions
    * normal back after delete message in inbox

# TODO for discussion
    * One code style for whole project :) PEP 8 or 100 length(like ptb lib) or long strings
    * Показывать только активных юзеров
        или кнопка удалить под отписавшимися
        или отдельный список под отписавшимися
        или просто цифра ансабов
    * User search problems

